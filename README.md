# Galaxyous Union AI Mix 🌌

**Galaxyous Union AI Mix** 是一个下一代企业级 AI 聚合平台，核心主打**多模型混合聚会 (AI Party)** 与 **复杂工作流编排**。

它不仅仅是一个聊天机器人，而是一个 **AI 社交网络**。在这里，你可以邀请 GPT-4、Claude 3.5、Gemini 1.5 Pro 等不同个性的 AI 模型加入同一个群聊，观看它们之间的辩论、协作甚至“勾心斗角”。

![Galaxyous Banner](https://via.placeholder.com/1200x400?text=Galaxyous+Union+AI+Mix)

## ✨ 核心亮点

### 1. 🎭 AI 聚会模式 (AI Party Mode) - **核心功能**
- **多模型群聊**：同时与多个顶尖大模型对话。从 OpenAI, Google 到 DeepSeek，所有模型同台竞技。
- **自动循环 (Auto-Loop)**：开启后，AI 之间将自主进行对话接龙，无需人类干预，围观 AI 之间的思想碰撞。
- **裁判系统 (Referee System)**：内置“裁判”角色（如 Judge 模式），能够自动主持辩论、判定胜负，甚至踢出表现不佳的模型。
- **社交拟真**：AI 支持发送“内心独白 (Thought)”、“私聊 (Whisper)”和“动作 (Action)”，还原真实社交场景。

### 2. 🧠 多模态创作中心 (Multimodal Center)
- **全能生成**：在同一个面板中即可完成文生图 (Image)、文生视频 (Video)、文生语音 (Audio) 和 视觉分析 (Analysis)。
- **实时通话 (Live)**：通过 Gemini Live API 实现与 AI 的低延迟实时语音通话。
- **历史回溯**：保存所有的创作历史，随时一键恢复灵感。

### 3. 🌊 可视化工作流引擎
- **拖拽式编排**：通过 React Flow 实现的直观界面，轻松构建复杂业务逻辑。
- **丰富节点**：支持 LLM、知识库检索 (RAG)、代码执行 (Sandbox)、MCP 工具调用等 16+ 种节点。
- **混合执行**：在一个工作流中混合使用不同的模型（如用 GPT-4 规划，用 Haiku 执行）。

### 4. 🔌 MCP (Model Context Protocol) 生态
- **三端统一**：工作区、个人中心和管理后台共享统一的 MCP 数据源。
- **工具市场**：内置 Web Search, File System, Code Executor 等实用工具。
- **安全沙箱**：所有 MCP 工具均在受控环境中运行，确保安全性。

### 5. 📚 RAG 知识库
- **企业级检索**：支持 PDF/Word/Txt 等多格式文档上传与解析。
- **混合检索**：结合向量检索 (Vector Search) 和关键词检索，精准召回上下文。

### 6. 🛡️ 企业级管理
- **用户分级**：支持 Free/Pro/Ultra 不同等级的模型访问权限控制。
- **裂变邀请**：内置裂变邀请机制，支持多级邀请奖励与数据统计。
- **配置模板**：支持一键导出/导入复杂的 AI 角色配置（加密保护）。

## 🛠️ 技术栈

- **Frontend**: React 19, Vite, TailwindCSS, React Flow, Lucide Icons
- **Backend**: Node.js, Express, Socket.IO (实时通讯)
- **Database**: MySQL (持久化), Redis (缓存/队列), BullMQ (任务调度)
- **AI Integration**: OpenAI SDK, Google GenAI SDK (Gemini), Anthropic SDK

## 🚀 快速开始

### 前置要求
- Node.js >= 18
- MySQL >= 8.0
- Redis >= 6.0

### 安装与运行

1. **克隆项目**
   ```bash
   git clone https://github.com/your-repo/galaxyous-union-ai-mix.git
   cd galaxyous-union-ai-mix
   ```

2. **安装依赖**
   ```bash
   npm install              # 前端依赖
   cd server && npm install # 后端依赖
   ```

3. **配置环境**
   复制 `server/.env.example` 到 `server/.env` 并填入数据库和 API Key。

4. **启动服务**
   ```bash
   # 终端 1 (后端)
   cd server && npm run dev

   # 终端 2 (前端)
   npm run dev
   ```

## 📦 部署

提供一键部署脚本：

```bash
chmod +x deploy.sh
./deploy.sh
```

## 📄 License

Aphace 2.0
